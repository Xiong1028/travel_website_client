import axios from 'axios';

export const handleUploadAction = (formData) => {
    return (dispatch) => {
        
/*        axios.post('/', formData).then( response => {
            dispatch(formData);  
        }) */
    }
}